import React from 'react';

// This component will be used later for reusable login form
const LoginForm: React.FC = () => {
    return (
        <div>
            {/* LoginForm component - to be implemented */}
        </div>
    );
};

export default LoginForm;
