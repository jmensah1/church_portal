import React from 'react';

// This component will be used later for navigation
const Navbar: React.FC = () => {
    return (
        <div>
            {/* Navbar component - to be implemented */}
        </div>
    );
};

export default Navbar;
